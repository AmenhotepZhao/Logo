package tokenizer;

/**
 * Enumerator class specifying the different types of tokens recognized
 * @author xiaolu
 *
 */
public enum TokenType { NAME, KEYWORD, NUMBER, SYMBOL, EOL, EOI, ERROR }
